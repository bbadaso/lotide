const raisinAlarm = function(cookie) {
// for (let element of cookie){
  //  if(cookie[element] === "🍇"){
    //  return "Raisin Alert";
 //   }
 // }
 for(let i=0; i<cookie.length; i++){
   if(cookie[i] === "🍇"){
     return "Raisin Alert"
   }
   
 }
   return "All good"
};

console.log(raisinAlarm(["🍫", "🍫", "🍇", "🍫"]));
console.log(raisinAlarm(["🍫", "🍇", "🍫", "🍫", "🍇"]));
console.log(raisinAlarm(["🍫", "🍫", "🍫"]));

//can create an empty array so you can update our empty array- hint
